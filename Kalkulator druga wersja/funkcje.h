#pragma once
#include <string>
#include <vector>

std::string logn(double liczba);

std::string odcs(double tablica[], int dlugosc);

std::string exponentt(double liczba);

std::string sredniawazona(std::vector<int> liczby, std::vector<int> wagi);